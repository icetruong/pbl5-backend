spring.application.name=pbl5

# Datasource (Render Postgres / local fallback)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/PBL5}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:123456}

jwt.secret=${JWT_SECRET:5W/gsQQnANpfq28MssMTclP3MUJ6blNTAMEvaKn/mq0=}
jwt.expiration=${JWT_EXPIRATION:86400000}

server.address=0.0.0.0
server.port=${PORT:8088}

app.base-url=${APP_BASE_URL:http://localhost:${server.port}}

ai.http.base-url=${AI_HTTP_BASE_URL:https://utdaica-pbl5-fruit-classifier.hf.space}
ai.http.api-key=${AI_API_KEY:}
ai.http.connect-timeout-ms=${AI_HTTP_CONNECT_TIMEOUT_MS:5000}
ai.http.read-timeout-ms=${AI_HTTP_READ_TIMEOUT_MS:60000}

storage.type=${STORAGE_TYPE:cloud}

# Cloudinary (set these on Render)
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:}
cloudinary.api-key=${CLOUDINARY_API_KEY:}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:}
cloudinary.secure=${CLOUDINARY_SECURE:true}
cloudinary.folder=${CLOUDINARY_FOLDER:pbl5}
